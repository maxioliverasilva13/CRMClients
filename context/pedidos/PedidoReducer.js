import {
  SELECCIONAR_CLIENTE,
  SELECCIONAR_PRODUCTO,
  CANTIDAD_PRODUCTOS,
} from "../../types/index";

export default (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
